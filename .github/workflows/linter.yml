name: 'Code checks'

on: [push]

env:
  cmssw_ver: CMSSW_10_2_10

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Run a CVMFS Docker image & set environment
      uses: Laurits7/CVMFS_docker
      # run:
      #   using: 'docker'
      #   image: 'docker://clelange/cc7-cmssw-cvmfs:latest'
      # uses: docker:/clelange/cc7-cmssw-cvmfs:latest
      # with:
      #   args: " -t - P --cap-add SYS_ADMIN --cap-add=MKNOD --security-opt apparmor=unconfined -e CVMFS_MOUNTS="cms.cern.ch" --device /dev/fuse -v /sys/fs/cgroup:/sys/fs/cgroup:ro /bin/bash"

